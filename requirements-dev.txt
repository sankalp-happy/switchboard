# Development and test dependencies.
#
#   pip install -r requirements-dev.txt
#
# pulls the runtime set in as well, so this one file is all a contributor needs.
# The Dockerfile deliberately installs requirements.txt only, keeping pytest out
# of the production image.

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
